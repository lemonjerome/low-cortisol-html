services:
  low-cortisol-html:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: low-cortisol-html-agent
    working_dir: /app
    volumes:
      - ..:/app
      - ${HOME}/Desktop/lch_workspaces:/root/Desktop/lch_workspaces
    environment:
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      WORKSPACE_ROOT: ""
      UI_HOST: 0.0.0.0
      UI_PORT: 8000
    ports:
      - "8000:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    stdin_open: true
    tty: true